const path = require('path');

module.exports = path.dirname(process.mainModule.filename)   // this will set path to mainModule (app.js) from where our app is being started,
                                                             // then file name to find out in which file, this module was launched up
